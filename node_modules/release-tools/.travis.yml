language: node_js

node_js:
  - 0.12
  - 4
  - 6

sudo: false

cache:
  directories:
    - node_modules

before_script:
  - git config --global user.name "Sascha Depold"
  - git config --global user.email "sdepold@dev.null"
